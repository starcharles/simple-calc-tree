require 'spec_helper'

describe STree do
  describe 'basic add' do
    it "return 1+2" do
      # address = MoneyTree::Address.new
      # expect(address.private_key.key.length).to eql(64)
    end
  end
end
